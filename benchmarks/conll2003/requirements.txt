# Environment of the reference run (Python 3.9.6, Apple M5 Max, macOS 26.6.2).
# The full list of installed packages is saved with each run as
# runs/<name>/environment-pip-freeze.txt. Install ucci itself from the
# repository root with: pip install -e .
torch==2.8.0
transformers==4.57.6
datasets==4.5.0
huggingface_hub==0.36.2
tokenizers==0.22.2
safetensors==0.7.0
numpy==2.0.2
scikit-learn==1.6.1
matplotlib==3.9.4
pytest==8.4.2
